
  # ExMart

  This is a code bundle for ExMart. The original project is available at https://www.figma.com/design/nscFxXagTMTOIwpgvRFORo/ExMart.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  